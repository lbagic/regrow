// Package scanner measures candidate paths and discovers targets:
// known per-OS paths, marker files (CACHEDIR.TAG, pyvenv.cfg,
// node_modules), and tool queries (docker df, simctl list, hf
// scan-cache, ollama list).
package scanner
